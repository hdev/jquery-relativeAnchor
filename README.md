jquery-relativeAnchor
=====================

relativeAnchor replaces the default behaviour of anchor links within overflowed containers with container scrolling.

Requirements
============

 * JQuery (>= 1.10.2)
 * underscore.js (>= 1.5.2)
